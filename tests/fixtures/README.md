`faiss-1.7.4.index` was generated on Windows / Python 3.11 using faiss-cpu 1.7.4
(the original application's dependency). It contains an `IndexIDMap(IndexFlatL2(2))`
with vectors `[1, 0]`, `[0, 1]` and SQLite IDs 7, 42. It contains no user data.
This fixture checks serialized index compatibility independently of the new writer.
